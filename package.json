{
  "name": "nova-remote",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix client",
    "start": "node server/server.js",
    "dev": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=18.x"
  }
}
